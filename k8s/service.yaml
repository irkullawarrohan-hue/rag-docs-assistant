apiVersion: v1
kind: Service
metadata:
  name: rag-docs-assistant
spec:
  selector:
    app: rag-docs-assistant
  ports:
    - name: api
      port: 8000
      targetPort: 8000
    - name: ui
      port: 8501
      targetPort: 8501
  type: NodePort