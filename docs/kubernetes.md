# Kubernetes

Kubernetes is a container orchestration platform.

A Pod is the smallest deployable unit in Kubernetes.

A Deployment manages replicas of Pods and maintains the desired number of replicas.

A Service provides a stable network endpoint for accessing Pods.

Kubernetes can automatically replace failed Pods to maintain the desired state.

kubectl is the command-line tool used to interact with a Kubernetes cluster.